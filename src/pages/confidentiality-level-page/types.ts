export type ConfidentialityLevel = {
  name: string;
  is_active: boolean;
  created_at: string;
  id: string;
};
export type ConfidentialityLevelFormValue = {
  name: string;
  is_active: boolean;
};
